//here we e discuss about the methods that are used in an array in js

let arr = [1,2,4,5,6];
//join() method is here used to make the array a single output
console.log(arr.join());
// here are there way to add an new Element first position of array

// arr.unshift("z");

// arr.splice(0,0,"z");

// arr =["z",...arr]


//to add and element on array at end we simply use push
arr.push('z')
arr.pop();//remove the element from array
console.log(arr)
